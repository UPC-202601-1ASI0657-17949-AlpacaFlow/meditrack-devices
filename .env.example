# Database (PostgreSQL — Render or local)
DB_URL=jdbc:postgresql://host:5432/meditrack_devices
DB_USERNAME=
DB_PASSWORD=

# Clinical — direct REST (server-to-server). Do NOT route through gateway.
CLINICAL_SERVICE_URL=https://meditrack-clinical-hph0avgbe2ejbyew.eastus-01.azurewebsites.net

# Threshold cache: reduces load on clinical during high-frequency IoT evaluation (seconds)
CLINICAL_THRESHOLD_CACHE_TTL_SECONDS=120
CLINICAL_CONNECT_TIMEOUT_SECONDS=3
CLINICAL_READ_TIMEOUT_SECONDS=5

# Prod profile only (Render Web Service)
# MYSQL_CONNECTION=
# MYSQL_USER=
# MYSQL_PASSWORD=
