# MQTT telemetry consumer (Mosquitto FOG layer)
# Run with: --spring.profiles.active=mqtt
# Requires broker at app.mqtt.broker-url (default tcp://localhost:1883)

app.mqtt.enabled=true
app.mqtt.broker-url=${MQTT_BROKER_URL:tcp://localhost:1883}
app.mqtt.topic=${MQTT_TELEMETRY_TOPIC:meditrack/devices/telemetry}
app.mqtt.client-id=${MQTT_CLIENT_ID:meditrack-devices}
app.mqtt.username=${MQTT_USERNAME:}
app.mqtt.password=${MQTT_PASSWORD:}
